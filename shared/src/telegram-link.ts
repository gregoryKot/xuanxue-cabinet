// Связка Telegram с уже существующим аккаунтом кабинета (ADR-0034): вошедший
// по почте (ADR-0029) не имеет telegramId, и бот не может привязать его видео
// экзамена к попытке — отправитель неизвестен (ADR-0023). Кнопка «Связать
// Telegram» выпускает одноразовый код для текущей сессии и уводит в чат с
// ботом по `t.me/<бот>?start=link_<код>`. Общий контракт api и web
// (CLAUDE.md «Слои»).

/** Формат кода — `randomBytes(16).toString('hex')`, 32 hex-символа (128 бит),
 * как у кода ссылки-приглашения (INVITE_CODE_RE): один и тот же приём, разный
 * смысл. Разбирается в start-payload.ts, выпускается в
 * TelegramLinkCodeService. */
export const TELEGRAM_LINK_CODE_RE = /^[0-9a-f]{32}$/;

/** Префикс payload `/start` для связки — третий deep link бота рядом с
 * `exam_<attemptId>` (ADR-0023) и `join_<code>` (ADR-0030). */
export const TELEGRAM_LINK_START_PREFIX = 'link_';

/** Ответ `POST /auth/telegram/link-code` (нужна сессия): готовый адрес чата с
 * ботом вместе с кодом внутри. Сам код наружу отдельным полем не идёт —
 * кабинету он нужен только внутри этой ссылки, и хранить его негде. */
export interface TelegramLinkCodeDto {
  telegramUrl: string;
}

/** Имя бота ещё не известно (бот не прогрелся или BOT_TOKEN не задан) — тот
 * же случай, что `telegramBotUsername` в AuthConfigDto: ссылку не собрать. */
export const TELEGRAM_LINK_NOT_AVAILABLE_MESSAGE =
  'Бот пока не подключён, связать аккаунт не получится. Напишите администратору школы.';

/** Код неизвестен, просрочен или уже использован — один текст на все три
 * причины (SECURITY §2, как у ссылки входа по почте). */
export const TELEGRAM_LINK_CODE_INVALID_MESSAGE =
  'Ссылка для связки устарела. Откройте кабинет и нажмите «Связать Telegram» ещё раз.';

/** Этот Telegram уже ключ входа другого человека. Молча перенести данные
 * (слияние) бот не имеет права: код мог прийти к человеку со стороны, и
 * слияние отдало бы чужой аккаунт вместе с ролями (ADR-0034, SECURITY §2).
 * Объединяет записи админ на «Людях». */
export const TELEGRAM_LINK_TAKEN_MESSAGE =
  'Этот Telegram уже связан с другим аккаунтом кабинета. Напишите администратору школы — он объединит записи.';

/** У аккаунта из кода уже есть другой Telegram: смена ключа входа — не
 * «связать», а отдельное решение, и делает его админ. */
export const TELEGRAM_LINK_OTHER_TELEGRAM_MESSAGE =
  'К этому аккаунту кабинета уже привязан другой Telegram. Напишите администратору школы, если его нужно сменить.';
