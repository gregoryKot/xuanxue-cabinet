// Заголовок ответа API с версией сборки, которую отдаёт сервер, — короткий
// SHA коммита (api/src/health/health-commit.ts). Вкладку кабинета держат
// открытой неделями (ADR-0076), а деплой уезжает по нескольку раз в день:
// фронт сравнивает значение с первым увиденным и, когда оно сменилось,
// предлагает перезагрузить страницу (ADR-0099).
export const APP_VERSION_HEADER = 'x-app-version';
