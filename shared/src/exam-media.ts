// Видео экзамена — контракт трёх путей привязки (ADR-0023, docs/PLAN.md §11
// слой 4.5): сообщением боту, ссылкой, вручную учителем. Байты не идут через
// нас — `ExamMediaDto` поэтому никогда не несёт `fileId`/`fileUniqueId`: они
// ведут прямо к видео в Telegram, а секретные поля документа наружу не
// возвращаются (CLAUDE.md «API»). Открыть видео учитель может в самом
// Telegram — бот пересылает его туда сразу при получении, в личный чат
// учителя с ботом (тот же канал, что предпросмотр и «Запись?»); отдельной
// ссылки «на конкретное сообщение» Telegram ботам не даёт, так что показывать
// в кабинете нечего, кроме факта и времени получения.
//
// `itemId` (ADR-0037) — видео теперь ответ на конкретный вопрос попытки, не
// вложение к попытке целиком: так учитель различает два видео-вопроса в одной
// форме, а ученик отвечает на вопрос там же, где он показан.

export const EXAM_MEDIA_KINDS = ['telegram', 'link', 'manual'] as const;
export type ExamMediaKind = (typeof EXAM_MEDIA_KINDS)[number];

export interface ExamMediaDto {
  id: string;
  attemptId: string;
  /** К какому вопросу попытки относится видео (ADR-0037). Необязателен: запись,
   * которую старый инстанс успел сделать без вопроса во время деплоя
   * (expand → contract), новый код обязан прочитать, а не отбросить. */
  itemId?: string;
  kind: ExamMediaKind;
  /** Только `kind: 'link'`. */
  url?: string;
  /** Оба — только `kind: 'telegram'`: Telegram отдаёт их в самом сообщении,
   * без обращения к видео (ADR-0023, `getFile` не зовём вовсе). */
  durationSec?: number;
  sizeBytes?: number;
  receivedAt: string; // ISO UTC с Z
  /** Короткая подпись — кто и как отметил вручную (`kind: 'manual'`). */
  note?: string;
}

export interface AddExamMediaLinkInput {
  url: string;
  /** Экран сдачи (AttemptQuestionVideo.tsx) присылает его всегда — форма
   * ссылки живёт у конкретного видео-вопроса. Необязательность в контракте
   * остаётся только ради деплоя: старый инстанс (expand → contract) ещё
   * работает без него, как и у `ExamMediaDto.itemId`. */
  itemId?: string;
}

export interface AddExamMediaManualInput {
  note?: string;
  /** См. `AddExamMediaLinkInput.itemId`. */
  itemId?: string;
}

export const EXAM_MEDIA_LIMITS = {
  url: 500,
  note: 300,
} as const;

// VOICE: что случилось и что сделать дальше, не «неверный формат».
export const EXAM_MEDIA_INVALID_URL_MESSAGE =
  'Это не похоже на ссылку на видео. Проверьте адрес и отправьте ещё раз.';

// «Попытка не найдена» переиспользует ATTEMPT_NOT_FOUND_MESSAGE (exams.ts) —
// тот же смысл и тот же текст для чужого и для несуществующего attemptId
// (SECURITY §3: не подтверждаем даже факт существования чужой попытки).

// Решение об уникальности: ссылка на ВОПРОС — одна (ADR-0037, было — одна на
// попытку; media-asset.schema.ts, частичный уникальный индекс
// `(attemptId, itemId)`). Повторная отправка ссылки на тот же вопрос получает
// этот отказ, а не тихий дубль, из которого потом непонятно, какую ссылку
// открывать. Видео из Telegram и ручные отметки не ограничены: «кружок» и
// обычное видео — два разных файла одного ответа, а поправленная подпись
// учителя — законная вторая отметка, не дубль.
//
// Совет «уберите» теперь выполним (ADR-0086): DELETE /attempts/:id/media/:mediaId
// снимает свою ссылку у вопроса, а POST /media/link уже есть — замена идёт тем
// же эндпоинтом, отдельного PUT нет. До ADR-0086 отказ отправлял к учителю, а
// механизма поправить ссылку не было ни у ученика, ни у него.
export const EXAM_MEDIA_ALREADY_LINKED_MESSAGE =
  'К этому вопросу ссылка уже прикреплена. Уберите её и пришлите новую, если прислали не ту.';

// ADR-0037: itemId обязан встретиться в снимке попытки и быть вопросом типа
// video — иначе видео пришло бы неизвестно к чему. Тот же случай для обеих
// причин отказа (вопроса нет и вопрос не video) — ученику не важно, какая
// именно, важно, что делать (VOICE: что случилось и что дальше).
export const EXAM_MEDIA_ITEM_NOT_FOUND_MESSAGE =
  'Не нашли этот вопрос в вашей попытке. Обновите страницу и попробуйте ещё раз.';

// ADR-0086: после `graded` ответ на вопрос не меняется — оценка учителя
// опирается ровно на то, что он видел, поэтому и добавление, и снятие ссылки
// отказывают одним и тем же текстом. Совет написать учителю честен: у штата
// школы тот же DELETE работает и после graded — поправит он, не автоматика.
export const EXAM_MEDIA_LOCKED_AFTER_GRADING_MESSAGE =
  'Работу уже проверили, менять ответ поздно. Если ошиблись — напишите учителю, он поправит.';
