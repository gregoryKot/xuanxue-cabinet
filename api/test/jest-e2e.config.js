// Конфиг e2e-прогона: поднимает реальный AppModule на MongoMemoryServer
// (test/e2e-support/create-app.ts), а не мокает сервисы — правило CLAUDE.md
// «новый эндпоинт = DTO + e2e на владение». rootDir указывает на api/, как и
// в основном api/jest.config.js, хотя сам файл лежит в api/test/.
module.exports = {
  rootDir: '..',
  testEnvironment: 'node',
  preset: 'ts-jest',
  testRegex: '.*\\.e2e-spec\\.ts$',
  // test/jest.setup.ts ставит ENCRYPTION_KEY в process.env ДО того, как jest
  // впервые потребует любой src/** — иначе статический импорт из фикстуры
  // e2e-теста (например test/e2e-support/lessons-fixtures.ts), исполняющийся
  // раньше beforeAll()/setTestEnv() (create-app.ts), тянет utils/encryption.ts
  // с пустым ключом: loadKeys() кэширует его на верхнем уровне модуля один
  // раз на весь прогон, и всё «шифрование» в e2e молча превращается в
  // открытый текст (ловушка, найдена ревью). Повторный setTestEnv() внутри
  // createTestApp() безобиден — ключ просто перезаписывается тем же способом.
  setupFiles: ['reflect-metadata', '<rootDir>/test/jest.setup.ts'],
  // Один воркер. Причина та же, что в api/jest.config.js, но лекарство строже:
  // e2e-набор поднимает в хуке не только mongod, но и целый AppModule, и
  // параллельных наборов память не держит — mongod не «не успевал стартовать»,
  // а падал на ходу с «MongoServerError: interrupted at shutdown».
  //
  // Замеры e2e на 8 ядрах / 8 ГБ 2026-09-17:
  //   половина ядер, хук 60 000 мс   все наборы упали
  //   один воркер, хук 60 000 мс     303 с, 48 наборов и 301 тест зелёные
  maxWorkers: 1,
  // Потолок памяти воркера — ADR-0070. Работает только потому, что прогон идёт
  // ВОРКЕРОМ: при `--runInBand` (был в скрипте `test:e2e`) все файлы живут в
  // одном процессе, а этот лимит jest игнорирует — он про воркеры. Каждый
  // e2e-файл оставляет после себя ~23 МБ (реестр модулей jest и кеш ts-jest
  // живут до конца прогона), и на 71-м файле дефолтный предел V8 в 2 ГБ
  // кончался: `FATAL ERROR: Reached heap limit`, SIGABRT, код 134. Само
  // приложение при этом не течёт — зонд «поднять и закрыть AppModule 8 раз
  // подряд в одном процессе» дал +0,6 МБ за цикл, а не +23.
  //
  // С воркером jest меряет его heap после каждого файла и перезапускает
  // воркер, переросший потолок: память держится числом ниже, а не числом
  // файлов. Замер 2026-09-20, heap ограничен 2 ГБ (дефолт V8 на машине, где
  // это и поймали), 77 файлов и 414 тестов:
  //   --runInBand            FATAL ERROR на 71-м файле, 138 с
  //   воркер, лимит 512 МБ   зелено, 167 с, пик heap 540 МБ, 17 перезапусков
  //   воркер, лимит 1 ГБ     зелено, 118 с, пик heap 1017 МБ, 3 перезапуска
  //
  // Взят гигабайт: вдвое ниже предела, который ронял прогон, и по времени
  // как здоровый `--runInBand` — за 512 МБ пришлось бы доплачивать 49 с на
  // каждый прогон, локальный и в CI, ради запаса, который и так есть.
  //
  // Без разделения на CI и не-CI, в отличие от api/jest.config.js: `--runInBand`
  // в скрипте делал прогон последовательным везде, так что CI ничего не теряет,
  // а потолок ему нужен не меньше — на раннере предел V8 выше, но конечен, и
  // без лимита в него упрётся тот же рост, просто на файле номер полтораста.
  workerIdleMemoryLimit: '1024MB',
  testTimeout: 60_000,
};
