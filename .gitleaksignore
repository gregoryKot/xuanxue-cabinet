# Fingerprints находок gitleaks, признанных ложными (формат: commit:file:rule:line).
# Добавлять только с объяснением; реальный секрет сюда не вносится — он отзывается.

# Тестовая заглушка JWT_SECRET в e2e-инфраструктуре (не секрет, значение публичное
# в репозитории с первого дня). В следующем коммите заменена на randomBytes, чтобы
# литерала в коде не было вовсе; запись нужна только для истории ветки этого PR.
7652ca1207d7c26f7205ad07b254f229961544e3:api/test/e2e-support/create-app.ts:generic-api-key:21

# Ключ объекта в хранилище («materials/<id>/<uuid>») в спеке уникального индекса:
# правило generic-api-key считает находкой «key = '<длинная строка>'». Не секрет —
# служебный адрес, и даже он в ответах API не появляется (ADR-0057). В следующем
# коммите ветки переменная переименована в objectPath, литерала рядом со словом
# «key» в коде больше нет; запись нужна только для истории этой ветки — gitleaks
# сканирует коммиты, а не рабочее дерево.
491f5f6432924faf3c0edeebc41406fe1b042614:api/src/common/model.registry.spec.ts:generic-api-key:71
