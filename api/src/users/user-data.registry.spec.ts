import { USER_OWNED_COLLECTIONS } from './user-data.registry';

// Сверка со схемами Mongoose: как только появится первая модель с полем
// userId (этап 1), список имён моделей, зарегистрированных в приложении,
// должен совпасть с USER_OWNED_COLLECTIONS — забытая модель падает здесь,
// а не обнаруживается инцидентом на проде (правило CLAUDE.md №4/чеклист
// «Новая коллекция»). Пока моделей нет, сверять не с чем — тест фиксирует
// точку приложения правила.
describe('USER_OWNED_COLLECTIONS', () => {
  it('существует и пока пуст — моделей с userId ещё нет (этап 0)', () => {
    expect(USER_OWNED_COLLECTIONS).toEqual([]);
  });
});
