{
  "name": "@xuanxue/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Типы, константы и чистые утилиты, общие для api и web.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^4.1.11"
  }
}
