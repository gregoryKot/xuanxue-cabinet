// Видео-вопрос на экране сдачи (ТЗ п.2): поля загрузки здесь нет и не будет —
// видео принимает бот, не кабинет (ADR-0023, байты через нас не идут). Строка
// говорит, что делать дальше: сдать работу и прислать видео боту, блок с
// кнопкой и ссылкой ждёт на экране «Отправлено» (AttemptMediaPrompt.tsx).
const VIDEO_NOTE =
  'Видео пришлёте боту после того, как отправите работу, — кнопка появится на следующем экране.';

export function AttemptQuestionVideo() {
  return (
    <p style={{ margin: 0, fontSize: 13, color: 'var(--ink-soft)' }}>{VIDEO_NOTE}</p>
  );
}
