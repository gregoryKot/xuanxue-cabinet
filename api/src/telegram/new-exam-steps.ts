// Шаги диалога «Собрать экзамен» в боте (ТЗ 4б.4, PLAN.md §12) — порядок,
// в котором учитель отвечает на вопросы: отметить вопросы, название, лимит
// времени, число попыток, срок сдачи (ADR-0125), подтверждение.
//
// 'pick' заводится сразу командой /экзамен (в отличие от examItemDraft, где
// до первой записи есть безсессионный screen 1): отметки копятся в
// bot_sessions с первого сообщения, второй инстанс при деплое должен их
// видеть.
//
// Отдельным файлом, а не строками в bot-session.schema.ts: та уже выше
// потолка в 150 строк, где храповик держит файл только на уменьшение
// (CLAUDE.md «Храповики»). Поднять её бейслайн ради нового шага значило бы
// выключить гейт для этого файла, а не решить задачу. Заодно список оказался
// там, где его и ищут, — рядом с диалогом, а не в схеме хранения.
export const NEW_EXAM_STEPS = [
  'pick',
  'title',
  'timeLimit',
  'attempts',
  'dueAt',
  'confirm',
] as const;

export type NewExamStep = (typeof NEW_EXAM_STEPS)[number];
