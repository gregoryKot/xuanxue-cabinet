root = true

[*]
charset = utf-8
end_of_line = lf
indent_style = space
indent_size = 2
insert_final_newline = true
trim_trailing_whitespace = true

# Markdown: висячие пробелы в конце строки — намеренный перенос строки (hard break).
[*.md]
trim_trailing_whitespace = false
