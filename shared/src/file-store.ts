// Тексты отказов объектного хранилища (ADR-0057) — рядом с остальными
// сообщениями контракта, а не в api: их видит человек, и переписывать их
// придётся вместе с экраном.
//
// Хранилище необязательно: без ключей R2 загрузка выключена, и экран прячет
// её заранее (`MeDto`/настройки, слой 3.10). Первое сообщение — на случай,
// когда кто-то всё же постучался в выключенный маршрут: запасной путь
// (ссылка на чужое облако) остался и работает.

// VOICE.md: что случилось и что делать дальше.
export const FILE_STORAGE_OFF_MESSAGE =
  'Хранилище файлов пока не подключено. Приложите материал ссылкой.';
export const FILE_STORAGE_FAILED_MESSAGE =
  'Файл не сохранился. Повторите загрузку через минуту.';
