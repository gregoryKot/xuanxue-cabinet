// Тело `PATCH /me/profile` — человек меняет своё имя (вход по почте заводит
// name, равным самой почте, см. email-login-user.service.ts). Отдельный файл,
// не auth.ts: тот и так близко к порогу файла-храповика (150 строк,
// CLAUDE.md «Храповики») — добавление этих трёх экспортов туда перевело бы
// его через порог, тот же приём выноса, что у OwnNameService в
// api/src/users/ (не метод в UsersService/большом auth.ts, а свой файл).

/** Предел длины имени в профиле. 80 — длиннее любого реального имени с
 * отчеством, короче, чем строка сломает список «Людей». */
export const PROFILE_LIMITS = { nameMax: 80 } as const;

/** Тело `PATCH /me/profile` — человек меняет только своё имя. Часового пояса
 * здесь нет намеренно: кабинет показывает время по часам браузера
 * (web/src/schedule/timezoneLabel.ts), и переключатель пояса в профиле
 * визуально ничего бы не менял. */
export interface UpdateProfileInput {
  name: string;
}

/** Текст ошибки пустого имени — по docs/VOICE.md: что не так и что сделать. */
export const PROFILE_NAME_REQUIRED_MESSAGE =
  'Впишите имя — по нему вас узнают учитель и другие ученики.';
