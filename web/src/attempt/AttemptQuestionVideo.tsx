// Видео-вопрос на экране сдачи (ТЗ п.2) — слой 4.5 ещё не построен: видео
// принимает бот, не кабинет. Честная строка вместо поля загрузки, которого
// у видео-вопроса быть не может (CLAUDE.md: демо-данные и выдуманные поля
// в рантайме не живут).
const VIDEO_NOTE = 'Видео примем в боте, это следующий шаг.';

export function AttemptQuestionVideo() {
  return (
    <p style={{ margin: 0, fontSize: 13, color: 'var(--ink-soft)' }}>{VIDEO_NOTE}</p>
  );
}
